# kindutil

```
❯ kindutil -h
Create kind cluster

Usage: kindutil [OPTIONS]

Options:
  -n, --name <NAME>                    Cluster name [default: kindutil]
  -c, --control-plane <CONTROL_PLANE>  [default: 1]
  -w, --worker <WORKER>                [default: 1]
  -d, --dir <DIR>                      Kind config directory [default: .kindutil]
      --delete                         Delete cluster only
  -h, --help                           Print help (see more with '--help')
  -V, --version                        Print version
```
